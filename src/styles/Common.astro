<style is:global>
  :root {
    --pink-panther: 244, 46, 117;
    --white-ghost: 248, 248, 248;
    --blue-navy: 39, 42, 73;
  }

  html {
    border-top: 8px solid rgb(var(--pink-panther));
  }
  body {
    margin: auto;
    width: calc(100% - 3rem);
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    background: rgb(var(--white-ghost));
  }

  main {
    flex: 1 1 0;
    display: flex;
    flex-direction: column;
    max-width: 900px;
    width: 100%;
    margin: auto;
  }

  p,
  h1,
  h2,
  h3,
  h4,
  h6,
  time {
    font-family: var(--font-fredoka), sans-serif;
    color: rgba(var(--blue-navy), 1);
  }

  a {
    font-family: var(--font-fredoka);
    color: #844063;
  }

  .button {
    background: rgba(var(--pink-panther), 1);
    padding: 0.5rem 2rem;
    border-radius: 50px;
    border: 7px double rgba(var(--white-ghost), 1);
    text-decoration: none;
    font-size: 0.8rem;
    color: rgb(var(--white-ghost));
    text-transform: uppercase;
    font-weight: 500;
  }

  .button:hover {
    border: 7px double #ffffff;
  }
</style>
